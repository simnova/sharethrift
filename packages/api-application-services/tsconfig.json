{
	"extends": ["../../tsconfig.base"],
	"compilerOptions": {
		/* Modules */
		"rootDir": ".",
		"types": ["node"],
		/* Emit */
		"outDir": "dist"
	},
	"include": ["src/**/*.ts"],
	"references": [
		//needs to match peer dependencies in package.json
		{ "path": "../api-context-spec" },
		{ "path": "../api-domain" }
	]
}
