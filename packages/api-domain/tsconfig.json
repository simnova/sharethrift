{
	"extends": ["../../tsconfig.base"],
	"compilerOptions": {
		/* Modules */
		"rootDir": ".",

		/* Emit */
		"outDir": "dist"
	},
	"include": ["src/**/*.ts"],
	"references": [
		{ "path": "../cellix-domain-seedwork" },
		{ "path": "../cellix-event-bus-seedwork-node" }
	]
}
