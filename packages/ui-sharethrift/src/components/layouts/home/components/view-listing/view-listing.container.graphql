# View Listing Query (backend-aligned)

query ViewListing($id: ID!) {
  viewListing(id: $id) {
    id
    title
    description
    category
    location
    sharingPeriodStart
    sharingPeriodEnd
    state
    images
    createdAt
    updatedAt
  }
}