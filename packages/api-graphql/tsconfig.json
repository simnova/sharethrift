{
	"extends": ["../../tsconfig.base"],
	"compilerOptions": {
		/* Modules */
		"rootDir": ".",
        "types": ["node"],
		/* Emit */
		"outDir": "dist"
	},
	"include": ["src/**/*.ts", "src/**/*.d.ts"],
	"references": [
		//needs to match peer dependencies in package.json
        { "path": "../api-application-services" },
		{ "path": "../api-context-spec" }
	]
}
