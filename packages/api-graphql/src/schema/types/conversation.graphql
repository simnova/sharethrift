
#graphql
"""
GraphQL schema for Conversations
"""

type Conversation {
  id: ObjectID!
  sharer: User!
  reserver: User!
  listing: Listing!
  twilioConversationId: String!
  schemaversion: Int!
  createdAt: String!
  updatedAt: String!
}

type User {
  id: ObjectID!
  username: String!
  # Add more fields as needed
}

type Listing {
  id: ObjectID!
  # Add more fields as needed
}

type Query {
  conversations: [Conversation!]!
  conversation(id: ObjectID!): Conversation
}

type Conversation {
  id: ObjectID!
  sharer: User!
  reserver: User!
  listing: Listing!
  twilioConversationId: String!
  schemaversion: Int!
  createdAt: String!
  updatedAt: String!
}

type Query {
  conversations: [Conversation!]!
  getUserConversations(userId: ObjectID!): [Conversation!]!
}

# type Mutation {
  # createConversation(sharerId: ObjectID!, reserverId: ObjectID!, listingId: ObjectID!): Conversation!
# }
