{
	"extends": ["../../tsconfig.base"],
	"compilerOptions": {
		/* Modules */
		"rootDir": ".",

		/* Emit */
		"outDir": "dist",
		/* add these to fix type error related to JSX */
		"types": ["@types/react"],
		"lib": ["dom", "esnext"]
	},
	"include": ["src/**/*.ts"],
	"references": [{ "path": "../api-domain" }]
}
