query ViewListingSharerInformationGetSharer($sharerId: ObjectID!) {
  personalUserById(id: $sharerId) {
    id
    userType
    isBlocked
    account {
      username
      profile {
        firstName
        lastName
        location {
          city
          state
          country
        }
      }
    }
  }
}