mutation PhotoUploadContainerUserCreateAuthHeaderForProfilePhoto($input: UserProfilePhotoImageInput!) {
  userCreateAuthHeaderForProfilePhoto(input: $input) {
    authHeader
    blobName
    requestDate
    isAuthorized
    errorMessage
  }
}